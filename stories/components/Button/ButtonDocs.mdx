import { Canvas, Meta, Story } from '@storybook/addon-docs';

# Button

With `MDX`, we can define a story for `Button` right in the middle of our
Markdown documentation.

<Canvas>
  <Story id="button--secondary"></Story>
</Canvas>